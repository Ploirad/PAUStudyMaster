# Backend API URL
# For local development, this points to your backend service
# In production, this will be automatically configured
REACT_APP_BACKEND_URL="http://localhost:8001"

# Note: Never commit the actual .env file with real values to version control